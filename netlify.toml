# Configuration Netlify — CONCEPT LOFT ERP
[build]
  command = "npm run build"
  publish = "dist"

# SPA : toutes les routes (devis, atelier, etc.) renvoient vers index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
